import Base from './Base';function getDate() {    const date = new Date();    const time = date.toString().split(' ')[4].slice(0, 5);    const day = date.getDate();    const month = date.toString().split(' ')[1];    const year = date.getFullYear();    return `${time}; ${day} ${month} ${year}`;}export default class WinnersAPI extends Base {    getWinners() {        return super.get(`winners`).then(res => res.data);    }    addWinners({winner, userName}) {        const data = {            winner: winner === 'user' ? userName : 'Computer',            date: getDate()        };        return super.post(`winners`, data).then(res => res.data);    }}