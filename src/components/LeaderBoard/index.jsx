import React, {useEffect, useState} from 'react';import { useDispatch, useSelector } from 'react-redux';import * as winnersAction from '../../store/actions/winners';import ListLeaders from './ListLeaders';export default () => {    const dispatch = useDispatch();    const winnersState = useSelector(state => state.winners);    const { winners, loaded } = winnersState;    const [growList] = useState(true);    useEffect(() => {        dispatch(winnersAction.getWinners())    }, [dispatch]);    return (        <div>            <h3>Leader Board</h3>            {!loaded                ?                <div/>                :                <ListLeaders                    fetchWinners={winners}                    growList={growList}                />            }        </div>    );}